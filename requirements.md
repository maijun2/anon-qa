# 匿名質疑応答アプリ 要件定義書

作成日: 2026-07-16
参考: 市場で発表されている質疑応答アプリの機能調査を反映

## 1. 目的

トレーニング(研修)中の質疑応答を**完全匿名**で行える Web アプリを Cloudflare 上に自作する。
Zoom のチャットでは質問者名が表示される問題を解消し、質問のハードルを下げる。

## 2. アーキテクチャ

| 要素 | 技術 | 用途 |
|---|---|---|
| フロントエンド | Cloudflare Pages | 参加者ページ / admin ページ(静的 HTML/JS) |
| API | Workers | REST API |
| リアルタイム通信 | Durable Objects (WebSocket) | 質問・投票・回答・アンケート結果の即時反映 |
| DB | D1 (SQLite) | セッション、質問、投票、回答、参考情報、アンケート、テンプレート |
| 画像ストレージ | R2 | 添付画像 — 質問・返信・講師回答(最大 5MB/枚) |
| スパム対策 | Turnstile | セッション入室時に 1 回検証 |
| 定期処理 | Cron Triggers | 30 日経過セッションの自動削除(D1 + R2) |

すべて Cloudflare 無料枠で運用可能な想定。

## 3. 機能要件

### 3.1 参加者(受講生)— アカウント登録不要・完全匿名

- アクセスコード(またはコード付き URL)で入室。Turnstile 検証は入室時に 1 回のみ
- **質問タブ**
  - テキスト質問の投稿
  - 画像添付: クリップボード貼り付け (Ctrl+V) + ファイル選択、最大 5MB/枚
  - いいね投票(1 人 1 票、ブラウザトークンで重複防止)
  - 自分の質問の編集 / 削除(ブラウザ内トークンで本人判定、匿名性は維持)
  - 講師の回答(テキスト / 画像)を質問にスレッド表示(「講師」ラベルで区別)
  - 返信スレッド: 参加者も質問にいつでも返信可(便乗質問・補足)。返信にも画像添付可(貼り付け / ファイル選択、最大 5MB/枚)。自分の返信は編集 / 削除可
  - 質問・返信・参考情報の本文中 URL は自動リンク化(新規タブで開く)
  - 添付画像はクリックでページ内ライトボックス表示(中央にフェードイン、背景クリック / × / Esc で閉じる)。⌘/Ctrl/中クリックでの別タブ表示は従来どおり
  - 絞り込み: 「未回答 / 回答済み / 自分の質問 / すべて」の 4 タブ + 件数バッジ(既定は未回答)。件数・絞り込みは読み込み済み質問に対するクライアント側処理(講師画面のサーバサイド絞り込みとは異なる)。絞り込みで一覧が短くなると番兵が可視域に入り、既存の追加読み込みで残りページが順次反映される
  - 回答済み質問は背景色 + バッジで判別(回答済みになるのは講師が回答したときのみ。参加者返信では変わらない)。本文テキストは回答済みでも濃色を維持し、メタ情報のみグレー
  - 自分の質問には「あなたの質問」バッジ + 淡い枠を表示
  - 未読表示: 自分が投稿 / 返信したスレッドに講師の新規回答が付くと「新しい回答」ドットを表示、カードのタップで既読化。未読状態はページ内メモリのみで管理(サーバ保存・localStorage 不使用 = 匿名性・簡潔性を維持)
  - 並び順: 新着順 / いいね順
  - 質問一覧は 50 件ずつのカーソルページネーション(スクロールで自動追加読み込み)
  - WebSocket による即時反映(更新・削除・投票は差分配信のためページネーションの影響を受けない)
  - 新着ピル通知: 他の参加者の新規質問は即時反映せずバッファし、「↑ 新しい質問が N 件」のピル(sticky)をクリックしたときに反映(閲覧位置のスクロール暴れ防止)。自分の投稿は従来どおり即時反映
  - 新着質問の通知音(🔔 ボタンで ON/OFF、既定 ON、設定は localStorage 保持。Web Audio で合成し外部アセット不要)
- **参考情報タブ**
  - 講師が登録したリンク / テキストメモを Module 別に閲覧
  - セッション有効期間中(30 日)いつでもアクセス可
- **アンケートタブ**
  - 講師が配信したアンケートに回答(単一選択 / 複数選択)
  - 集計結果をリアルタイムでグラフ表示(講師・参加者の両方)

### 3.2 講師(admin)— パスワード保護

- **セッション管理**
  - セッション作成(コース名 + 開催日)→ アクセスコード自動発行
  - コードはセッションごとに使い捨て。ダッシュボードで確認可
  - セッション終了 / 手動削除
- **質問管理**
  - トリアージ: 「未回答 / 回答済み / すべて」の 3 タブ + 件数バッジ(既定は未回答)
  - 並べ替え: いいね順(既定)/ 新着順 / 古い順
  - キーワード検索: 本文の部分一致(サーバサイド、`session_id` スコープ限定。D1 の LIKE で実装、FTS5 は将来課題)。入力は 250ms デバウンス
  - 絞り込み・並べ替え・検索はサーバサイド(`status` / `sort` / `q` クエリ)。新着順・古い順は 50 件ずつの keyset カーソル、いいね順・検索時は vote 数が可変でカーソル化できないため上限 500 件の一括取得
  - 2 ペイン表示: PC 幅では左 = 質問一覧(密なカード: いいね数 + 本文 2 行)/ 右 = 詳細 + 回答入力(sticky)。左右に対の見出しを置き白枠の上端を揃える。狭幅では 1 カラムにフォールバックし、選択時に詳細を表示
  - 回答の投稿(テキスト / 画像添付: 貼り付け・ファイル選択、最大 5MB/枚。参加者全員に表示、投稿と同時に回答済みになる)
  - 回答済みマーク、質問の削除
  - 一括モデレーション: 選択モードで複数質問をチェック → 画面内の確認 UI を経て一括削除。既存の単一削除ロジック(R2 画像の後始末 + `question:deleted` broadcast)を 1 件ずつ呼ぶ
  - 返信のモデレーション削除(参加者返信・講師回答とも返信単位で削除可)
  - 新着ピル通知: 新規質問は即時反映せずバッファし、「↑ 新しい未回答が N 件」のピルをクリックしたときに現在のフィルタ・ソートに整合する位置へ反映(件数バッジは受信時点で更新)
  - 新着質問の通知音(参加者ページと同じ 🔔 トグル。投影モードにもあり)
- **参考情報管理**
  - リンク(URL 付き)/ テキストメモの登録
  - Module 別分類、表示順の並び替え
- **アンケート管理**
  - 質問文 + 選択肢の作成(複数選択許可オプション)
  - 下書きの編集(配信後は編集不可)
  - 既存アンケート(下書き / 配信中 / 終了)の「複製」: 内容を作成フォームに転記し、そのまま / 修正して新しい下書きを作る(同一セッション内で似たアンケートを繰り返す用途)
  - 「配信」で参加者にリアルタイム配信、即時集計表示
- **テンプレート**
  - 参考情報・アンケートを事前登録
  - セッション作成時にテンプレート選択で自動コピー(繰り返し研修向け)
- **外部連携(オプション)**
  - `GET /api/admin/pending-questions`: 未回答質問一覧を返す外部連携 API。`Authorization: Bearer <PENDING_API_TOKEN>` で認証(admin Cookie とは独立)。`status='active'` のセッションかつ `is_answered=false` の質問のみ、新着順で最大 50 件。講師が未回答質問を外部ツールへ一括で取り出して確認するための連携口(回答の作成・投稿は講師が本アプリの管理画面で行い、本 API は取得専用で書き込みはしない)
  - Discord Webhook 通知: 質問投稿時に「質問が来た」ことへの気づき用として Discord へ通知(本文は 200 文字まで、画像添付時は明記)。`DISCORD_WEBHOOK_URL` 未設定時は無効。送信失敗は投稿処理に影響させない(fire-and-forget)

## 4. 非機能要件

### 4.1 匿名性

- IP アドレス・User-Agent を DB に保存しない
- ブラウザトークン(ランダム UUID)は本人の投稿(質問・返信)の編集/削除判定と投票の重複防止のみに使用し、個人と紐付けない
- 参加者の返信もハッシュのみ保存し、講師を含め誰も質問者・返信者を特定できない
- pending-questions API・Discord Webhook 通知(3.2 外部連携)も同様に IP・User-Agent・匿名トークン(ハッシュ含む)を一切含めない

### 4.2 データ保持

- セッションは作成から **30 日で自動削除**(質問・画像・アンケート・参考情報を含む全データ)
- Cron Triggers で日次実行、R2 の画像も併せて削除

### 4.3 セキュリティ / 荒らし対策

- Turnstile: 入室時に 1 回検証(投稿ごとには出さない、UX 優先)
- Rate limit: 投稿・返信・投票の連投防止。キーは「IP + 端末単位の匿名トークンハッシュ」の複合
  (教室 NAT で同一 IP を共有する参加者同士が制限を食い合わないため)。トークンなしの
  リクエストは IP 単独キー + 緩い上限(通常値の 10 倍)。IP・ハッシュとも保存しない(DO メモリ内のみ)
- admin ログインの失敗は「IP 別(5 失敗/60s)+ グローバル全 IP 合算(20 失敗/60s)」の 2 段で制限。
  グローバル超過時は完全遮断ではなく指数バックオフ(30s → 60s → … → 上限 10 分)
- HTML ページに Content-Security-Policy を付与(script は自ホストと Turnstile のみ許可。
  値は `src/http.ts` の `HTML_CSP` と `public/_headers` で管理、変更時は両方を同期)
- 添付画像は PNG / JPEG / GIF / WebP のみ許可(SVG はストアド XSS 対策で拒否)。Content-Type と 5MB 制限をフロント・サーバの両方で検証
- アクセスコードを知っていれば誰でも入室可能なため、共有範囲に注意(運用ルール)
- admin はパスワード認証(環境変数 + セッション Cookie)
- pending-questions API(外部連携)は専用の `PENDING_API_TOKEN`(環境変数)による Bearer 認証。admin パスワードや `APP_SECRET`(admin Cookie / 入室トークンの HMAC 署名用)とは分離し、漏洩時の影響範囲を限定する

## 5. clasmo との差別化ポイント

- いいね投票による質問の優先度付け(clasmo には無い)
- 自分の質問の編集 / 削除(clasmo には無い)
- セルフホストのため、データの完全なコントロールとカスタマイズが可能

## 6. IaC / デプロイ(手作業最小化)

方針: **Wrangler 中心の IaC** + GitHub Actions による CI/CD。

### 6.1 IaC 構成

- `wrangler.jsonc` に以下をすべて宣言的に定義し、`wrangler deploy` で一括反映
  - Workers(エントリポイント、互換性日付)
  - D1 binding
  - R2 binding
  - Durable Objects binding + migrations
  - Cron Triggers(日次の期限切れセッション削除)
  - Pages との統合(Workers Static Assets を採用し、Pages ではなく Worker 単体で静的配信 + API を統合することも可 → **推奨**。デプロイ単位が 1 つになり構成が簡素化)
- D1 スキーマは `migrations/` ディレクトリで管理し、`wrangler d1 migrations apply` で適用
- 初回のみ必要なリソース作成(D1 database / R2 bucket / Turnstile widget)は `scripts/setup.sh` にまとめる
  - D1 / R2: `wrangler d1 create` / `wrangler r2 bucket create`
  - Turnstile widget: Cloudflare API (`POST /accounts/{account_id}/challenges/widgets`) で作成
- シークレット(admin パスワード、Turnstile secret key)は `wrangler secret put` で登録(コードには含めない)
  - オプション(外部連携): `PENDING_API_TOKEN`(pending-questions API の Bearer 認証)、`DISCORD_WEBHOOK_URL` / `DISCORD_THREAD_ID`(Discord Webhook 通知)。未設定でも本体機能に影響しない

### 6.2 CI/CD(GitHub Actions)

- `main` ブランチへの push で自動デプロイ
  - `cloudflare/wrangler-action` を使用
  - ステップ: D1 migrations apply → wrangler deploy
- PR 時: lint + ユニットテスト(vitest + coverage 計測、レポートを artifact 保存)+ E2E(`wrangler dev` をローカル起動 + Playwright chromium)。deploy ジョブは E2E に依存させない(Turnstile 実 API 等の外部ネットワーク起因のフレーキーさでデプロイを止めないため)
- 必要な GitHub Secrets: `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`

### 6.3 残る手作業(初回のみ)

1. Cloudflare アカウント作成 + API Token 発行(Workers/D1/R2/Turnstile 権限)
2. GitHub リポジトリ作成 + Secrets 登録(2 個)
3. `scripts/setup.sh` の実行(リソース作成 + シークレット登録)

以降の変更はすべて git push のみで反映される。

### 6.4 リポジトリ構成(案)

```
anon-qa/
├── wrangler.jsonc          # IaC 本体(bindings, DO, cron)
├── src/
│   ├── index.ts            # Worker エントリ(API + 静的配信)
│   ├── session-do.ts       # Durable Object(WebSocket)
│   └── ...
├── public/                 # 静的アセット(参加者/admin ページ)
├── migrations/             # D1 スキーマ(連番 SQL)
├── scripts/setup.sh        # 初回リソース作成
├── .github/workflows/deploy.yml
└── README.md
```

## 7. 画面構成

| 画面 | パス(案) | 内容 |
|---|---|---|
| 入室 | `/` | アクセスコード入力 + Turnstile |
| 参加者 | `/s/:code` | 質問 / 参考情報 / アンケート の 3 タブ |
| 講師ログイン | `/admin` | パスワード入力 |
| ダッシュボード | `/admin/dashboard` | セッション / テンプレート一覧・作成 |
| セッション管理 | `/admin/s/:id` | 質問管理 / 参考情報 / アンケート配信 |
| 投影モード | `/admin/s/:id/present` | 質問 / アンケート結果を大画面表示(Zoom 画面共有・プロジェクタ用) |

## 8. デザイン・対応環境

### 8.1 対応デバイス

- **PC + スマートフォン両対応**(レスポンシブデザイン)
  - ブレークポイント: モバイル < 768px ≦ タブレット/PC
  - 参加者ページはモバイルファーストで設計(会場型研修で手元スマホから質問するケースを考慮)
  - admin ページは PC 前提のレイアウトで可(ただしモバイルでも崩れないこと)
- タップターゲットは 44px 以上(投票ボタン等)
- 画像貼り付け: PC は Ctrl+V / ドラッグ&ドロップ、モバイルはファイル選択(カメラ撮影含む)

### 8.2 対応ブラウザ

- Chrome / Edge / Safari / Firefox の最新 2 バージョン(モダンブラウザのみ)
- IE・レガシーブラウザは非対応。Polyfill 不要、ES2022+ / CSS Grid / Flexbox を素直に使用

### 8.3 デザインテイスト

- **シンプル・ライト**: 白基調 + アクセントカラー 1 色(青系を想定、CSS カスタムプロパティで一元管理し変更容易に)
- フォント: システムフォントスタック(游ゴシック / Hiragino / Segoe UI 等)。Web フォント不使用(表示速度優先)
- ダークモードは初期リリースでは対応しない(CSS 変数化により将来対応可能な構造にはしておく)

### 8.4 投影モード(講師用)

- `/admin/s/:id/present` で質問一覧・アンケート結果を**大きな文字で表示する専用ビュー**
- 用途: Zoom 画面共有、会場プロジェクタ投影
- 要件:
  - 文字サイズは通常画面の約 1.5〜2 倍、高コントラスト
  - 質問は「いいね順 / 新着順」切替、回答済みは非表示切替。返信スレッドは講師 / 参加者のラベル付きで表示
  - アンケート結果は棒グラフで大きく表示、リアルタイム更新
  - 新着質問の通知音トグル(🔔)あり
  - 操作 UI は最小限(投影中に管理操作は通常の admin 画面で行う想定)

### 8.5 アクセシビリティ / UX

- キーボード操作のみで質問投稿が完結すること(Enter 送信、フォーカス管理)
- 質問・返信の本文は主役として濃色(WCAG AA 4.5:1 以上)で表示し、タイムスタンプ等のメタ情報のみ薄いグレーにする(投影・明るい教室でも可読)
- 色のみに依存した状態表現をしない(回答済み = 背景色 + ラベル表示、未読 = ドット + テキスト)
- WebSocket 切断時は自動再接続 + 「再接続中」の表示
- 投稿・投票などの操作は楽観的 UI 更新(体感速度優先)+ 失敗時ロールバック
- 他者の新着で閲覧位置を勝手に動かさない(新着ピルに溜めてクリックで反映。参加者・講師とも)
- 画像ライトボックスはキーボードのみで完結(Esc で閉じ、閉じたら元の要素へフォーカス復帰。開いている間は背面をスクロールさせない)
- 破壊的な一括操作は画面内の確認 UI を挟む(ブラウザモーダルの `confirm` はイベント処理をブロックするため一括削除では使わない)

## 9. 実装時の注意(Claude Code への引き継ぎ事項)

- アーキテクチャは Worker 単体構成(Workers Static Assets)を推奨。Pages を分ける場合は CORS 設定が必要
- WebSocket は Durable Objects の Hibernation API を使用し、無料枠の duration 課金を抑える
- 画像は Worker 経由で R2 に PUT(presigned URL は使わずシンプルに)。Content-Type 検証 + 5MB 制限をサーバ側でも実施
- ブラウザトークンは `crypto.randomUUID()` を localStorage に保持。トークンはハッシュ化して D1 に保存
- Rate limit は DO 内のメモリカウンタで実装(キーは IP + 匿名トークンハッシュの複合。storage へ永続化しない)
- Cron: `0 3 * * *`(JST 12:00)に期限切れセッションの D1 行 + R2 オブジェクトを削除
